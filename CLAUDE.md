# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 프로젝트 개요

실무 백엔드 개발자를 위한 개념서. 실무에서 마주치는 세 가지 질문에 답한다.

- **선택** — 이 상황에서 A와 B 중 무엇을 골라야 하는가
- **문제 해결** — 이 증상이 나왔을 때 원인과 해법은 무엇인가
- **기술 이해** — 이 기술은 어떤 문제를 풀기 위해 존재하고, 언제 쓰는가

## 작업 시작 전 필수

**모든 작업 전에 반드시 두 파일을 읽는다:**

1. `규칙/지침서.md` — 집필 원칙, 문체, 코드 규칙, 에이전트 프로토콜, 금지 목록 전체
2. `규칙/토픽맵.md` — 집필 대상 목록 및 현재 진행 상태

판단이 필요할 때는 지침서 원칙을 근거로 결정하고 어떤 원칙을 적용했는지 밝힌다. 지침서로 결정할 수 없는 문제는 추측하지 말고 물어본다.

**규칙 우선순위:** 사용자의 즉석 지시 > 지침서 > 일반 관행
단, `지침서.md §13 금지 목록`은 사용자가 명시적으로 해제하기 전까지 최우선.

## 파일 구조

```
.
├── CLAUDE.md
├── 규칙/
│   ├── 지침서.md       ← 집필 지침 (작업 전 필독)
│   └── 토픽맵.md       ← 토픽 목록 및 진행 상태 (작업 전 필독)
├── README.md
├── docs/
│   ├── 01-linux-network/
│   ├── 02-infra-cloud/
│   ├── 03-container/
│   ├── 04-cicd/
│   ├── 05-java/
│   ├── 06-spring/
│   ├── 07-database/
│   ├── 08-system-design/
│   └── 09-operations/
└── assets/
    └── diagrams/
```

파일명: `docs/<카테고리>/<번호>-<영문-슬러그>.md` (예: `docs/06-spring/04-proxy-aop.md`)

## 새 챕터 작업 순서

지침서 §11 프로토콜을 반드시 따른다:

1. 토픽맵에 있는 주제인지 확인 (없으면 먼저 물어본다)
2. 이 문서가 답할 "하나의 질문"을 한 문장으로 선언
3. §6 골격으로 목차만 먼저 제시하고 **승인받는다** ← 생략 금지
4. 승인 후 집필
5. §12 체크리스트 항목별 통과/실패 보고

**한 세션에 한 챕터.** 여러 챕터 동시 집필 금지.

## 핵심 제약

- 파일당 500줄 상한 — 넘으면 쪼갠다
- 프론트매터 필수: `title`, `category`, `level`, `tags`, `updated`, `verified`, `versions`, `sources`
- 수치·벤치마크는 출처 링크 필수 — 없으면 삭제
- `verified: false`인 문서는 README 목차에 ⚠️ 표시
- 다이어그램은 Mermaid (노드 7개 이하), 이미지 파일 사용 금지
- 문체: `~합니다`체 통일
